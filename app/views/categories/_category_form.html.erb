<%= bootstrap_form_for( @category, url: '/categories', remote: true, layout: :horizontal) do |i_f| %>
  <%= i_f.text_field :name%>
  <%= i_f.submit t('category.create_new'), class: "btn btn-success"%>
<% end %>